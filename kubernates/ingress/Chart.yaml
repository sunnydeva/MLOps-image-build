apiVersion: v2
name: ingress
description: Wrapper Helm chart for NGINX Ingress Controller
type: application

version: 0.1.0
appVersion: "1.10.0"

dependencies:
  - name: ingress-nginx
    version: "4.10.0"
    repository: "file://charts"